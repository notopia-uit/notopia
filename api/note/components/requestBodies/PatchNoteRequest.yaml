# vim: ft=yaml.openapi
description: Patch note request (partial update)
required: true
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Note.yaml
        - example:
            $ref: ../examples/PatchNoteRequest.yaml
