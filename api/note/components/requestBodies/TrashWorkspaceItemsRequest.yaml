# vim: ft=yaml.openapi
required: true
content:
  application/json:
    schema:
      type: array
      items:
        oneOf:
          - $ref: ../schemas/TrashedNote.yaml
          - $ref: ../schemas/TrashedFolder.yaml
        discriminator:
          propertyName: type
          mapping:
            note: ../schemas/TrashedNote.yaml
            folder: ../schemas/TrashedFolder.yaml
