# vim: ft=yaml.openapi
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        name:
          $ref: ../schemas/Tag.yaml#/properties/name
        limit:
          type: integer
          default: 10
          example: 10
      required:
        - name
