# vim: ft=yaml.openapi
required: true
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/NoteRequired.yaml
        - example:
            $ref: ../examples/CreateNoteRequest.yaml
