# vim: ft=yaml.openapi
required: true
content:
  application/json:
    schema:
      type: array
      items:
        type: object
        properties:
          userId:
            $ref: ../schemas/User.yaml#/properties/id
          role:
            $ref: ../schemas/WorkspaceRole.yaml
        required:
          - userId
          - role
