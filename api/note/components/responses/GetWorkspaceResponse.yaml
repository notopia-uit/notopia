# vim: ft=yaml.openapi
description: OK
content:
  application/json:
    schema:
      allOf:
        - $ref: ../schemas/Workspace.yaml
        - type: object
          properties:
            rootFolder:
              $ref: ../schemas/FolderWithRelations.yaml
          required:
            - rootFolder
