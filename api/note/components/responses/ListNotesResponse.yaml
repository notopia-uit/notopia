# vim: ft=yaml.openapi
description: Successful response
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: ../schemas/Note.yaml
          # HACK: got warning "Example value must conform to the schema: type must be array."
          # from redocly "no-invalid-schema-examples"
          # So moved example to x-example and hope other tools can still use it.
          x-example:
            $ref: ../examples/ListNoteResponseData.yaml
        pagination:
          allOf:
            - $ref: ../../../shared/components/schemas/Pagination.yaml
            - description: Pagination details for the list of notes
              example:
                $ref: ../examples/ListNoteResponsePagination.yaml
      required:
        - data
        - pagination
