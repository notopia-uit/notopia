# vim: ft=yaml.openapi
description: Revisions
content:
  application/json:
    schema:
      type: object
      properties:
        data:
          type: array
          items:
            $ref: ../schemas/Revision.yaml
        pagination:
          $ref: ../../../common/components/schemas/Pagination.yaml
      required:
        - data
        - pagination
      # example:
