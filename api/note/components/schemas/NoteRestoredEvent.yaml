# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - NoteRestoredEvent
    example: NoteRestoredEvent
  data:
    type: object
    x-go-type-name: NoteRestoredEventData
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
    required:
      - id
required:
  - type
  - data
