# vim: ft=yaml.openapi
type: object

properties:
  nodes:
    type: array
    items:
      type: object
      properties:
        id:
          type: string
          format: uuid
          description: Can be either a note ID or a hashtag prefixed tag name
          example: 46f94d4d-41b5-4df6-bf8d-270fb9a49c97
        name:
          type: string
          description: Can be either a note name or a hashtag prefixed tag name (duplicate with id)
          example: Meeting Notes
        type:
          type: string
          enum:
            - note
            - tag
          example: note
        weight:
          type: number
          description: Optional weight for the note node, rounded to 1 decimal place
          minimum: 0
          maximum: 1
          example: 0.8
      required:
        - id
        - name
        - type

  links:
    type: array
    items:
      type: object
      properties:
        source:
          type: string
          description: The ID of the source node (note id or hashtag prefixed tag name)
          example: 46f94d4d-41b5-4df6-bf8d-270fb9a49c97
        target:
          type: string
          description: The ID of the target node (note id or hashtag prefixed tag name)
          example: 46f94d4d-41b5-4df6-bf8d-270fb9a49c97
