# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - NoteTrashedEvent
    example: NoteTrashedEvent
  data:
    type: object
    x-go-type-name: NoteTrashedEventData
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
    required:
      - id
required:
  - type
  - data
