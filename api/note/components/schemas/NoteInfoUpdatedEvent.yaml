# vim: ft=yaml.openapi
type: object

properties:
  type:
    type: string
    enum:
      - NoteInfoUpdatedEvent
    example: NoteInfoUpdatedEvent

  data:
    type: object
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
      name:
        $ref: ./Note.yaml#/properties/name
      icon:
        $ref: ./Note.yaml#/properties/icon
      folderId:
        $ref: ./Note.yaml#/properties/folderId
      tags:
        $ref: ./Note.yaml#/properties/tags

    required:
      - id
      - name
      - icon

required:
  - type
  - data
