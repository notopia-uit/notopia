# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - NoteCreatedEvent
    example: NoteCreatedEvent
  data:
    type: object
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
      name:
        $ref: ./Note.yaml#/properties/name
    required:
      - id
      - name
required:
  - type
  - data
