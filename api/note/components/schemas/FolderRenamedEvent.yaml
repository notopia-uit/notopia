# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - FolderRenamedEvent
    example: FolderRenamedEvent
  data:
    type: object
    properties:
      id:
        $ref: ./Folder.yaml#/properties/id
      oldName:
        $ref: ./Folder.yaml#/properties/name
        example: Work
      newName:
        $ref: ./Folder.yaml#/properties/name
        example: Working
    required:
      - id
      - newName
required:
  - type
  - data
