# vim: ft=yaml.openapi
type: object

properties:
  type:
    type: string
    enum:
      - NoteUpdatedEvent
    example: NoteUpdatedEvent

  data:
    type: object
    x-go-type-name: NoteUpdatedEventData
    allOf:
      - $ref: ./Note.yaml

required:
  - type
  - data
