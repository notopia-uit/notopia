# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - NoteContentUpdatedEvent
    example: NoteContentUpdatedEvent
  data:
    type: object
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
    required:
      - id
required:
  - type
  - data
