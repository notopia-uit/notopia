# vim: ft=yaml.openapi
type: object

description: >-
  When user A is in read mode, user B commits changes,
  user A will receive this event

properties:
  type:
    type: string
    enum:
      - NoteContentUpdatedEvent
    example: NoteContentUpdatedEvent
  data:
    type: object
    properties:
      id:
        $ref: ./Note.yaml#/properties/id
    required:
      - id

required:
  - type
  - data
