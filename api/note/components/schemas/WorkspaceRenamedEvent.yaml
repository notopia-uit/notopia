# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - WorkspaceRenamedEvent
    example: WorkspaceRenamedEvent
  data:
    type: object
    properties:
      id:
        $ref: ./Workspace.yaml#/properties/id
      oldName:
        $ref: ./Workspace.yaml#/properties/name
      newName:
        $ref: ./Workspace.yaml#/properties/name
        example: New Workspace Name
    required:
      - id
      - oldName
      - newName
required:
  - type
  - data
