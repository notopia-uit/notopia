# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - FolderRestoredEvent
    example: FolderRestoredEvent
  data:
    type: object
    x-go-type-name: FolderRestoredEventData
    properties:
      id:
        $ref: ./Folder.yaml#/properties/id
    required:
      - id
required:
  - type
  - data
