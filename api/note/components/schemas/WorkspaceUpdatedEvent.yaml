# vim: ft=yaml.openapi
type: object

properties:
  type:
    type: string
    enum:
      - WorkspaceUpdatedEvent
    example: WorkspaceUpdatedEvent

  data:
    $ref: ./Workspace.yaml

required:
  - type
  - data
