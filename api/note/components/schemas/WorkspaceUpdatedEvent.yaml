# vim: ft=yaml.openapi
type: object

properties:
  type:
    type: string
    enum:
      - WorkspaceUpdatedEvent
    example: WorkspaceUpdatedEvent

  data:
    type: object
    x-go-type-name: WorkspaceUpdatedEventData
    allOf:
      - $ref: ./Workspace.yaml

required:
  - type
  - data
