# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - FolderCreatedEvent
    example: FolderCreatedEvent
  data:
    type: object
    x-go-type-name: FolderCreatedEventData
    properties:
      id:
        $ref: ../schemas/Folder.yaml#/properties/id
      name:
        $ref: ../schemas/Folder.yaml#/properties/name
      icon:
        $ref: ../schemas/Folder.yaml#/properties/icon
    required:
      - id
      - name
required:
  - type
  - data
