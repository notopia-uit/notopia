# vim: ft=yaml.openapi
type: object
properties:
  type:
    type: string
    enum:
      - FolderTrashedEvent
    example: FolderTrashedEvent
  data:
    type: object
    x-go-type-name: FolderTrashedEventData
    properties:
      id:
        $ref: ./Folder.yaml#/properties/id
    required:
      - id
required:
  - type
  - data
