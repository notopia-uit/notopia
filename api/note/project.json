{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "api/note",
  "projectType": "application",
  "tags": ["scope:api", "type:openapi"],
  "targets": {
    "bundle:yaml": {
      "executor": "nx:run-commands",
      "options": {
        "command": "redocly bundle note --config ../redocly.yaml -o ./note.bundled.yaml",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/note.bundled.yaml"],
      "inputs": ["openapiFilesWithCommon"]
    },
    "bundle:json": {
      "executor": "nx:run-commands",
      "options": {
        "command": "redocly bundle note --config ../redocly.yaml -o ./note.bundled.json",
        "cwd": "{projectRoot}"
      },
      "outputs": ["{projectRoot}/note.bundled.json"],
      "inputs": ["openapiFilesWithCommon"]
    },
    "build": {
      "dependsOn": ["bundle:yaml", "bundle:json"]
    },
    "watch-bundle:json": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm nx watch --projects=api/note -- pnpm nx bundle:json \\$NX_PROJECT_NAME",
        "forwardAllArgs": false,
        "cwd": "{projectRoot}"
      },
      "continuous": true
    },
    "dev": {
      "executor": "nx:run-commands",
      "options": {
        "command": "scalar document serve -w ./note.bundled.json --port 9081",
        "forwardAllArgs": false,
        "cwd": "{projectRoot}"
      },
      "continuous": true,
      "dependsOn": ["bundle:json", "watch-bundle:json"]
    },
    "preview": {
      "executor": "nx:run-commands",
      "options": {
        "command": "scalar document serve ./note.bundled.json --port 9081",
        "cwd": "{projectRoot}"
      },
      "continuous": true,
      "dependsOn": ["bundle:json"]
    },
    "mock": {
      "executor": "nx:run-commands",
      "options": {
        "command": "scalar document mock -w ./note.bundled.json --port 8081",
        "cwd": "{projectRoot}"
      },
      "continuous": true,
      "dependsOn": ["bundle:json"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "redocly lint note --config ../redocly.yaml",
        "cwd": "{projectRoot}"
      },
      "inputs": ["openapiFilesWithCommon"]
    },
    "gen:go": {
      "inputs": ["{projectRoot}/oapi-codegen.yaml", "openapiFiles"],
      "outputs": ["{workspaceRoot}/pkg/api/note/note.gen.go"],
      "executor": "nx:run-commands",
      "options": {
        "command": "oapi-codegen --config ./oapi-codegen.yaml ./note.yaml",
        "cwd": "{projectRoot}"
      }
    },
    "gen": {
      "dependsOn": ["gen:go"]
    }
  },
  "implicitDependencies": ["api/common"]
}
