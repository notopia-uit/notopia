# vim: ft=yaml.openapi
$ref: ./EventBase.yaml
properties:
  data:
    type: object
    properties:
      id:
        $ref: ../../../note/components/schemas/Note.yaml#/properties/id
    required:
      - id
