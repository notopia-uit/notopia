# vim: ft=yaml.openapi
type: object

properties:
  id:
    type: string
    format: uuid

  name:
    type: string

  plainTextContent:
    type: string
    description: Plain text content

  tags:
    type: array
    items:
      type: string

required:
  - id
  - name
