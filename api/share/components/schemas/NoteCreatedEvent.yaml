# vim: ft=yaml.openapi
type: object

properties:
  id:
    type: string
    format: uuid

  workspaceId:
    type: string
    format: uuid

  name:
    type: string

  icon:
    type: string
    nullable: true

required:
  - id
  - workspaceId
  - name
  - icon
