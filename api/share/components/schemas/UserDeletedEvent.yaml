# vim: ft=yaml.openapi
allOf:
  - $ref: ./EventBase.yaml
  - properties:
      data:
        type: object
        properties:
          userId:
            $ref: ../../../common/components/schemas/User.yaml#/properties/id
        required:
          - userId
