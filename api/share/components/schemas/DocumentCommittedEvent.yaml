# vim: ft=yaml.openapi
allOf:
  - $ref: ../../../document/components/schemas/Document.yaml
  - properties:
      userId:
        type: string
        format: number

      tags:
        type: array
        items:
          type: string

      outgoingLinkIds:
        type: array
        items:
          type: string
          format: uuid

    required:
      - userId
      - tags
      - outgoingLinkIds
