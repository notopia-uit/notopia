## yaml-language-server: $schema=https://www.schemastore.org/openapi-3.X.json
# vim: ft=yaml.openapi
openapi: 3.0.4

components:
  schemas:
    NoteMoved:
      $ref: ./NoteMoved.yaml
    FolderMoved:
      $ref: ./FolderMoved.yaml
