{
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "name": "api",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:noop",
      "dependsOn": [
        {
          "target": "build",
          "projects": ["api/note"]
        }
      ]
    }
  }
}
