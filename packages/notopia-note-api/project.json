{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "packages/notopia-note-api",
  "projectType": "library",
  "targets": {
    "gen": {
      "executor": "nx:run-commands",
      "options": {
        "command": "pnpm gen",
        "cwd": "{projectRoot}"
      },
      "dependsOn": [
        {
          "projects": "api/note",
          "target": "build:json"
        }
      ]
    }
  }
}
