# yaml-language-server: $schema=https://www.schemastore.org/traefik-v3-file-provider.json
http:
  routers:
    note:
      entryPoints:
        - web
      rule: Host(`web.notopia.localhost`)
      service: note
      middlewares:
        - jwt

  services:
    note:
      loadBalancer:
        servers:
          - url: http://host.docker.internal:3000
