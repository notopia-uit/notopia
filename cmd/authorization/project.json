{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "authorization",
  "projectType": "application",
  "tags": ["scope:service", "type:go"],
  "namedInputs": {
    "default": [
      "goPkgFiles",
      "goFiles",
      "{workspaceRoot}/api/**/*.go",
      "{workspaceRoot}/internal/authorization/**/*.go"
    ]
  },
  "targets": {
    "gen:wire": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "outputs": ["{projectRoot}/wire_gen.go"],
      "options": {
        "command": "wire",
        "cwd": "{projectRoot}"
      },
      "cache": false
    },
    "dev": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "options": {
        "command": "go run .",
        "cwd": "{projectRoot}",
        "tty": true
      },
      "continuous": true
    },
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "options": {
        "command": "go build -o ./authorization .",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["gen:wire", "^gen*", "^build*"]
    },
    "preview": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "options": {
        "command": "./authorization",
        "cwd": "{projectRoot}",
        "tty": true
      },
      "continuous": true,
      "dependsOn": ["build"]
    },
    "debug": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "options": {
        "command": "dlv debug --headless --listen=127.0.0.1:23459 --accept-multiclient",
        "cwd": "{projectRoot}",
        "tty": true
      },
      "dependsOn": ["gen:wire", "^gen*"],
      "continuous": true
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "golangci-lint run ./...",
        "cwd": "{projectRoot}"
      },
      "cache": false,
      "parallelism": false
    },
    "tidy": {
      "executor": "nx:run-commands",
      "options": {
        "command": "go mod tidy"
      }
    },
    "nx-release-publish": {
      "parallelism": false
    },
    "test": {}
  },
  "implicitDependencies": ["pkg", "internal/authorization"],
  "release": {
    "docker": {
      "repositoryName": "notopia-uit/authorization"
    }
  }
}
