{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "note",
  "projectType": "application",
  "targets": {
    "gen:wire": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/wire_gen.go"],
      "options": {
        "command": "wire",
        "cwd": "{projectRoot}"
      },
      "cache": false
    },
    "build": {
      "executor": "nx:run-commands",
      "inputs": ["goFiles"],
      "outputs": ["{projectRoot}/dist/note"],
      "options": {
        "command": "go build -o ./dist/note .",
        "cwd": "{projectRoot}"
      },
      "dependsOn": ["gen:wire", "^gen*", "^build*"]
    },
    "preview": {
      "executor": "nx:run-commands",
      "inputs": ["goFiles"],
      "options": {
        "command": "./dist/note",
        "cwd": "{projectRoot}",
        "tty": true
      },
      "continuous": true,
      "dependsOn": ["build"]
    },
    "run": {
      "executor": "nx:run-commands",
      "inputs": ["goFiles"],
      "options": {
        "command": "go run -gcflags='all=-N -l' .",
        "cwd": "{projectRoot}",
        "tty": true
      },
      "dependsOn": ["gen:wire", "^gen*"],
      "continuous": true
    },
    "serve": {
      "dependsOn": ["run"]
    },
    "lint": {
      "executor": "nx:run-commands",
      "inputs": ["goFiles"],
      "options": {
        "command": "golangci-lint run .",
        "cwd": "{projectRoot}"
      },
      "parallelism": false
    },
    "tidy": {
      "executor": "nx:run-commands",
      "inputs": ["goFiles"],
      "options": {
        "command": "go mod tidy",
        "cwd": "{projectRoot}"
      }
    },
    "docker:build": {
      "inputs": ["goFiles"]
    },
    "docker:run": {
      "inputs": ["goFiles"]
    },
    "goose": {
      "executor": "nx:run-commands",
      "options": {
        "command": "goose",
        "cwd": "{projectRoot}"
      }
    },
    "migration:create": {
      "executor": "nx:run-commands",
      "options": {
        "command": "goose create",
        "cwd": "{projectRoot}"
      }
    },
    "migration:up": {
      "executor": "nx:run-commands",
      "options": {
        "command": "goose up",
        "cwd": "{projectRoot}"
      }
    },
    "migration:down": {
      "executor": "nx:run-commands",
      "options": {
        "command": "goose down",
        "cwd": "{projectRoot}"
      }
    },
    "migration:validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "goose validate",
        "cwd": "{projectRoot}"
      }
    },
    "lint:migration": {
      "dependsOn": ["migration:validate"]
    },
    "gen:sqlc": {
      "inputs": [
        "{workspaceRoot}/pkg/note/infra/persistence/pgsqlc/*.sql",
        "{workspaceRoot}/pkg/note/migration/",
        "{projectRoot}/sqlc.yaml"
      ],
      "outputs": ["{workspaceRoot}/pkg/note/infra/persistence/pgsqlc/*.go"],
      "executor": "nx:run-commands",
      "options": {
        "command": "sqlc generate",
        "cwd": "{projectRoot}"
      },
      "cache": true
    }
  },
  "implicitDependencies": ["pkg/noteopenapi", "pkg/pb"]
}
