{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "internal/authorization",
  "projectType": "library",
  "tags": ["scope:library", "type:go"],
  "namedInputs": {
    "default": ["{projectRoot}/**/*.go", "{projectRoot}/model.conf", "!{projectRoot}/_test.go"]
  },
  "targets": {
    "gen:mockery": {
      "executor": "nx:run-commands",
      "inputs": ["default"],
      "options": {
        "command": "mockery",
        "cwd": "{projectRoot}"
      }
    }
  },
  "implicitDependencies": ["pkg"]
}
