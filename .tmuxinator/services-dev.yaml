# yaml-language-server: $schema=https://www.schemastore.org/tmuxinator.json
name: notopia svcs dev

windows:
  - svcs dev:
      layout: tiled
      panes:
        - note:
            - tmux set-option -w pane-border-status top
            - mise exec -- nx run note --no-tui
        - authorization:
            - mise exec -- nx run authorization --no-tui
        - document:
            - mise exec -- nx run document --no-tui
        - search-worker:
            - mise exec -- nx run search-worker --no-tui
