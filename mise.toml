#:schema https://raw.githubusercontent.com/jdx/mise/refs/heads/main/schema/mise.json

[env]
REDOCLY_TELEMETRY = "off"
REDOCLY_SUPPRESS_UPDATE_NOTICE = "true"
MISE_EXPERIMENTAL = 1

[tools]
"github:pressly/goose" = "3.27.1"
"go:github.com/goforj/wire/cmd/wire" = "v1.1.0"
"go:github.com/oapi-codegen/oapi-codegen/v2/cmd/oapi-codegen" = "2.6.0"
buf = "1.65.0"
pnpm = "10.33.2"
sqlc = "1.30.0"
java = "latest"
mockery = "3.7.0"
"aqua:go-delve/delve" = "1.26.3"

[shell_alias]
nx = "pnpm exec nx"
shadcn = "pnpm exec shadcn"
tsgo = "pnpm exec tsgo"
compose = "docker compose"
p = "pnpm"

# [deps.pnpm]
# auto = true
#
# [deps.go]
# auto = true
#
# [deps.buf]
# auto = true
# sources = ["./buf.yaml"]
# outputs = ["./buf.lock"]
# run = "buf dep update"
# description = "Manage buf dependencies"
